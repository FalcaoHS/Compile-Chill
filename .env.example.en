# ============================================
# Compile & Chill - Environment Variables
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root
# 2. Replace example values with your actual values
# 3. NEVER commit the .env file to Git (already in .gitignore)
#
# ============================================

# ============================================
# Database Connection
# ============================================
# PostgreSQL connection string
# Example with Neon: postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/compileandchill?sslmode=require
# Local example: postgresql://user:password@localhost:5432/compileandchill?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/compileandchill?schema=public"

# ============================================
# NextAuth Configuration
# ============================================
# Base URL of the application
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# Secret for session encryption
# Generate with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET="paste-here-the-secret-generated-with-openssl-or-online-tool"

# ============================================
# X (Twitter) OAuth 2.0 Credentials
# ============================================
# Get these credentials at: https://developer.twitter.com/en/portal/dashboard
# IMPORTANT: Use OAuth 2.0 credentials (not API Key/Secret)
# Configure Callback URI: http://localhost:3000/api/auth/callback/twitter
X_CLIENT_ID="paste-here-the-oauth-2-0-client-id-from-twitter"
X_CLIENT_SECRET="paste-here-the-oauth-2-0-client-secret-from-twitter"

# ============================================
# Upstash Redis (Rate Limiting)
# ============================================
# Optional for development, recommended for production
# Get it at: https://upstash.com/
# Create a Redis database and copy the URL and Token
UPSTASH_REDIS_REST_URL="paste-here-the-upstash-redis-url"
UPSTASH_REDIS_REST_TOKEN="paste-here-the-upstash-redis-token"

# ============================================
# Node Environment
# ============================================
# Automatic in production (Vercel), but can be set manually
# NODE_ENV="development"  # or "production"
